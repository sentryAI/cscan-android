apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    //Java libraries for use with Android Studio need to be built with Java 7
    sourceCompatibility = 1.7
}